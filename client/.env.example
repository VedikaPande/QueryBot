# Client environment variables
# Copy to `.env` (or `.env.local`) and adjust.

# Base URL of the Flask API, including the /api prefix.
# The browser talks only to this service; dataset operations are proxied through
# it so the SQLite service is never exposed publicly.
VITE_API_BASE_URL=http://localhost:5000/api
